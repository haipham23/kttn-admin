{
  "create": {
    "type": "object",
    "properties": {
      "content": {
        "type": "string",
        "minLength": 1
      },
      "answers": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "minItems": 3,
        "maxItems": 5,
        "uniqueItems": true
      },
      "result": {
        "type": "number",
        "minimum": 1,
        "maximum": { "$data": "1/answers/length" }
      }
    },
    "required": ["content", "answers", "result"]
  },
  "update": {
    "properties": {
      "_id": {
        "type": "string"
      },
      "content": {
        "type": "string"
      },
      "answers": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "minItems": 3,
        "maxItems": 5,
        "uniqueItems": true
      },
      "result": {
        "type": "number",
        "minimum": 1,
        "maximum": { "$data": "1/answers/length" }
      }
    },
    "required": ["_id", "content"]
  },
  "fetch": {
    "properties": {
      "skip": {
        "type": "number",
        "minimum": 0
      },
      "limit": {
        "type": "number",
        "minimum": 0
      }
    },
    "required": ["skip", "limit"]
  },
  "toggle": {
    "properties": {
      "_id": {
        "type": "string"
      },
      "isActive": {
        "type": "boolean"
      }
    },
    "required": ["_id", "isActiver"]
  },
  "remove": {
    "properties": {
      "_id": {
        "type": "string"
      }
    },
    "required": ["_id"]
  }
}
